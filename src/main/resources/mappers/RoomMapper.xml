<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dgit.dao.RoomDao">
	<sql id="selectBySql">
		select * from room r, forest f where r.for_no=f.for_no
	</sql>
	<sql id="whereForestSql">
		<where>
			<if test=""></if>
		</where>
	</sql>

	<select id="selectByAll" parameterType="Room" resultType="Room">
		<include refid="selectBySql" />
	</select>
	
	
	
	
	
	
	<select id="selectRoomByIdAll" parameterType="Room" resultType="Room">
		select r.r_name, r.r_pax from room r, reservation s where r.r_no = #{r_no} and s.u_id = #{u_id};
	</select>
	<select id="selectRoomByNO" parameterType="Room" resultType="Room">
		select r.r_name, r.r_pax from room r, reservation s where r.r_no = #{r_no} and s.res_no = #{res_no};
	</select>
</mapper>